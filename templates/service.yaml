apiVersion: v1
kind: Service
metadata:
  name: {{ include "helm-base-chart.fullname" . }}
  labels:
    {{- include "helm-base-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "helm-base-chart.selectorLabels" . | nindent 4 }}
